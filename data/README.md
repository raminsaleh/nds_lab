# Data Guide
- تمام داده‌ها در Google Drive نگهداری می‌شوند.
- قالب CSV استاندارد: `time,open,high,low,close` (UTC، مرتب صعودی).
- نمونهٔ پردازش‌شده در `refs/sources.yaml` لینک شده است.
- داده‌های خام در `data/raw/` و نسخه‌های پاک‌سازی‌شده در `data/processed/` نگهداری می‌شوند (در Drive).
